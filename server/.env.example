# Server environment variables (do NOT store secrets in the repository)
# Copy to .env and fill in values locally

MONGODB_URI=
CLERK_SECRET_KEY=
TMDB_API_KEY=
SMTP_USER=
SMTP_PASS=
SENDER_EMAIL=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NODE_ENV=development
PORT=3000
